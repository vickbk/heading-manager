/**
 * Re-exports the CLI task runner used to guard script execution and delegate fatal failures.
 */
export { getErrorMessage } from "./utils/get-error-message";
export { runTask } from "./utils/run-task";
